<h1>State List</h1>

<p>here is a list of states...</p>

<!-- iterate through each state in the array and put them into list items individually -->

<select>
	<% @states.each do | s | %>
	<%# this works but it's too much repetition. add an if block inline %>
	<option value= <%= s['id'] %> <% if s['id'] == 'CA' %> selected <% end %>><%= s['name'] %></option>
	<% end %>
</select>
